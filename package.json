{
  "name": "gym-tracker-deploy",
  "private": true,
  "description": "Dokku/Heroku entry: real app lives in ./gym-tracker",
  "scripts": {
    "build": "npm --prefix gym-tracker ci && cd gym-tracker && npx expo export --platform web",
    "start": "./gym-tracker/node_modules/.bin/serve gym-tracker/dist -s -l tcp://0.0.0.0:$PORT"
  },
  "engines": {
    "node": "24.x"
  }
}
